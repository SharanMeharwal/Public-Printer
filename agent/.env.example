# ===================================
# Printer Agent Environment Variables
# ===================================

# Cloud Server URL
# For local development:
SERVER_URL=http://localhost:3000

# For production (update with your server's IP or domain):
# SERVER_URL=http://192.168.1.100:3000
# SERVER_URL=https://yourserver.com

# Printer Name
# MUST match the exact printer name in your system
# Find it in: Settings → Devices → Printers & scanners (Windows)
# Or run: wmic printer get name (Windows CMD)
# Or run: lpstat -p -d (Linux)
PRINTER_NAME=HP-LaserJet-Pro

# Download Directory for PDFs
DOWNLOAD_DIR=./downloads
